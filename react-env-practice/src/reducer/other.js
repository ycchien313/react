const initState = { otherList: ['其他事情1', '其他事情2'] };

const otherReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default otherReducer;
